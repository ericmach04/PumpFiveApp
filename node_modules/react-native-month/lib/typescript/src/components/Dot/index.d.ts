import { ViewStyle } from 'react-native';
import { DayDot, DotTheme } from '../../types';
interface Props extends DayDot, DotTheme {
    active: boolean;
    style: ViewStyle;
}
declare function Dot({ active, color, selectedColor, dotContainerStyle, style, }: Props): JSX.Element;
export default Dot;
