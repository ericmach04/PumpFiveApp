import React from 'react';
import { DayType } from 'react-native-calendario';
declare type Props = {};
declare type State = {
    minDate?: Date;
    maxDate?: Date;
    startDate?: Date;
    endDate?: Date;
};
export default class App extends React.PureComponent<Props, State> {
    state: {
        disableRange: boolean;
        startDate: Date;
        endDate: Date;
        minDate: Date;
        maxDate: Date;
    };
    handlePress: (date: Date) => void;
    renderDayContent: (item: DayType) => JSX.Element;
    render(): JSX.Element;
}
export {};
